import React, { Component } from 'react';

const Title = () => (
  <div>
    <h1 className="title-container__title">Weather Finder</h1>
    <p className="title-container__subtitle">Enter your location to find current weather</p>
  </div>
);

export default Title;